{
  "name": "learning-loop",
  "version": "1.41.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "description": "Self-improving learning loop across Claude sessions",
  "scripts": {
    "test": "node --test --test-force-exit tests/*.test.mjs tests/**/*.test.mjs",
    "lint": "eslint .",
    "bench": "node bench/bench.mjs",
    "bench:quick": "node bench/bench.mjs --quick",
    "bench:baseline": "node bench/bench.mjs --save-baseline",
    "test:mutation": "stryker run stryker.file-lock.json && stryker run stryker.deny-match.json && stryker run stryker.shadow-gate.json && stryker run stryker.cite-extract.json && stryker run stryker.edge-classifier.json && stryker run stryker.inject.json && stryker run stryker.artifact-verify.json"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^9.6.1",
    "eslint": "^9.39.4",
    "prettier": "3.8.3"
  }
}
