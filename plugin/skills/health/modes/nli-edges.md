# NLI Edges Mode (removed)

The `--nli-edges` mode and the NLI contradiction-edge subsystem it tuned have been removed. Run `node plugin/scripts/nli-cleanup.mjs` to clean residual edges, promotion-log columns, and vault frontmatter from existing installs.
