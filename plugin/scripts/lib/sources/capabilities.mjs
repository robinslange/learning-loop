export const CAPABILITIES = Object.freeze(['query', 'fetch', 'verify']);

export const capabilityMethods = Object.freeze({
  query: ['query'],
  fetch: ['fetch'],
  verify: ['matches', 'verify'],
});
